# 이상기체 상태 방정식 기획서 — Codex 적대적 리뷰 1차

- 리뷰어: OpenAI Codex (gpt-5.6-sol, reasoning effort: high)
- 일자: 2026-07-25
- 대상: `이상기체 상태 방정식.md` + 참조 코드 7개 파일 (전부 실제 대조 완료)
- 결과: 치명 3 · 중대 14 · 경미 4 — **판정: 재설계 필요**

---

- `[치명]` 2D 충돌량을 곧바로 `atm` 압력으로 부르는 모델은 차원이 정의되지 않았고, “측정값 대 이론값” 비교도 순환적이다.

  근거: 기획서 `§1.2`, `이상기체 상태 방정식.md:61–74`는 2D 벽에 전달된 충격량을 벽 “넓이”로 나눈 뒤 기준 상태에서 1 atm이 되도록 보정한다. 그러나 2D 엔진의 압력은 힘/길이이고 3D 압력은 힘/면적이다. 유효 깊이, 길이·시간 척도, 분자 질량, 대표입자 가중치가 없다. 보정 상수 하나로 상태 간 비율은 맞출 수 있지만 이는 `atm` 측정기가 아니라 기준점에 맞춘 압력 프록시다. 더구나 피스톤 목표 부피부터 `V_eq=nRT/P외부`로 계산한다(`:80`). 그 값으로 다시 `PV/nT=R`을 만들고 “데이터에서 귀납”한다고 하는 것은 독립 검증이 아니다(`:328–340`).

  **구체적인 수정 제안:** 둘 중 하나를 선택해야 한다.

  1. 수업용 단순안: `P이론=nRT/V`를 상태값으로 쓰고, 충돌 엔진은 `상대 충돌 압력 P̃/P̃₀`, 충돌 빈도, 평균 충격량만 표시한다. “측정 atm”과 “실험으로 법칙 증명” 문구를 삭제한다.
  2. 물리 측정안: 유효 깊이 `d`, 물리적 벽 면적, 픽셀-미터·프레임-초 변환, 기체 종류와 분자 질량, 대표입자 가중치 `w=nN_A/N_sim`을 명시하고  
     `P₃D = Σ(w Δp_n)/(A_wall Δt)`로 계산한다. 표에는 평균값과 표준오차까지 기록한다.

- `[치명]` 강제로 움직이는 피스톤에서 일이 사라지며, “감쇠 끄기 → 단열 가열”은 현재 모델로 구현할 수 없다.

  근거: 벽 충돌은 단순 법선 반전(`:66`), 피스톤은 방정식으로 정한 목표 위치로 감쇠 이동(`:76–82`), 온도 변경은 모든 속도에 `√(T_new/T_old)`를 곱한다(`:67`). 움직이는 벽과의 충돌은 벽 기준에서 계산하여 `v'_n=2u_wall-v_n`가 되어야 한다. 등온 과정도 `ΔU=0`일 뿐 일 `W`가 0인 것이 아니며, 그만큼 열 `Q`가 출입한다. 등압 가열에서는 `W=PΔV=nRΔT`가 반드시 존재한다. 단순 속도 재조정은 내부에너지 변화만 만들고 피스톤 일은 처리하지 않는다. 감쇠는 피스톤 운동의 수치 설정이지 단열 조건이 아니다.

  **구체적인 수정 제안:** 이 단원의 목적이 상태관계라면 `:82`의 단열 맛보기를 삭제하고, 피스톤을 “입자 힘으로 움직이는 물체”가 아니라 “새 평형 상태를 보여 주는 비동역학적 전환 애니메이션”이라고 명시한다. 입자가 피스톤을 밀어 올린다는 인과 문구(`:293–294`)도 삭제한다. 실제 과정을 보여 주려면 움직이는 벽 충돌, 열저장고/thermostat, `Q·W·ΔU` 에너지 장부를 별도로 구현해야 한다.

- `[치명]` “입자 간 충돌 없음 = 이상기체 가정”이라는 설명은 틀렸고, 맥스웰–볼츠만 분포의 차원도 결정되지 않았다.

  근거: `:62–65`는 입자 충돌을 제거한 이유를 이상기체 가정이라고 설명한다. 이상기체가 무시하는 것은 평상시 분자 간 퍼텐셜과 분자 자체 부피이지, 평형을 만드는 탄성 충돌까지 반드시 제거한다는 뜻이 아니다. 충돌 없는 계는 처음부터 맥스웰 분포이면 고정 벽에서 그 분포가 유지되지만, 임의의 비평형 분포나 피스톤 압축 후의 방향 비등방성은 평형으로 이완하지 않는다. 또한 3D 속력분포를 뽑아 2D 방향으로 배치하면 `⟨v_x²⟩=3kT/(2m)`가 되어 2D 압력이 50% 크게 나온다.

  **구체적인 수정 제안:** 2D 엔진이면 `v_x,v_y ~ Normal(0,kT/m)`으로 뽑아 2D 맥스웰 분포를 사용한다. 평형 회복이 필요하면 저비용 Andersen thermostat처럼 일정 확률로 속도 성분을 재표본화하거나, 80개 이하이므로 공간 해시를 이용한 탄성 원판 충돌을 구현한다. 충돌을 계속 생략한다면 “동역학 시뮬레이션”이 아니라 “각 평형 상태의 입자 시각화”라고 한정하고 상태 변경 후 전 속도를 재표본화한다.

- `[중대]` “고정 1 + 조절 1 + 반응 1”이라는 뼈대는 일부 단계에서만 성립하며 ⑥의 “네 조작 모두”는 다시 상태방정식 위반을 허용한다.

  근거: ③–⑤는 표에 쓰이지 않은 `n`도 항상 고정되므로 실제로는 `n 고정 + T/P/V 중 하나 고정 + 하나 조절 + 하나 계산`이다(`:48–53`). ⑥ 입자수 모드는 P와 T 두 개를 고정한다. 따라서 `:493`의 요약은 정확하지 않다. `:56`, `:322–324`의 자유 모드에서 P·V·T·n을 모두 직접 조절할 수 있으면 처음 비판한 과잉 자유도 문제가 재발한다.

  **구체적인 수정 제안:** 모드별 제약행렬을 명세한다. ⑥에서도 학생이 먼저 종속변수 하나를 선택하게 하고 그 변수의 컨트롤만 비활성화하여 자동 계산한다. “네 변수의 값을 모두 볼 수 있다”와 “네 변수를 모두 독립 조작한다”를 구분한다.

- `[중대]` 노이즈가 있는 측정 압력을 사용하면서 `PV/nT`가 항상 정확히 `0.0821`이라고 제시한 것은 자기모순이다.

  근거: `:73`은 0.7초 이동평균에도 측정 노이즈가 있다고 명시하지만, `:328–339`는 모든 행이 정확히 0.0821이라고 한다. 외부 설정압력을 쓰면 정확한 값이 나오지만 충돌 측정 결과가 아니고, 충돌 압력을 쓰면 행마다 값이 달라진다.

  **구체적인 수정 제안:** 표에 `P설정`, `P충돌 평균±불확도`를 분리한다. 학생이 계산하는 R은 충돌 평균으로 구해 여러 값과 평균 `R̄`, 기준값 대비 오차율을 표시하고 “오차 범위에서 일정하다”를 선택하게 한다. 정확히 0.0821을 보여 줄 목적이면 `P설정`으로 계산하되 열 제목을 “모형의 이론값”으로 바꾼다. 0.7초 대신 최근 200회 이상 피스톤 충돌 또는 3–5초 표본을 권장한다.

- `[중대]` `입자 40개=1 mol`은 분자 모형과 실제 계산을 연결한 것이 아니라 대표입자 의미를 감추고 있다.

  근거: `:91–95`대로라면 화면의 점 하나는 0.025 mol, 약 `1.5×10²²`개 분자를 대표한다. 이 점에 실제 분자 질량과 맥스웰 속도를 적용하면서 충격량에는 대표 가중치를 적용하지 않으면 압력 계산이 틀린다. 반대로 점 자체의 거대 질량을 속력분포에 사용해도 틀린다. n 슬라이더 간격도 명세되지 않았다.

  **구체적인 수정 제안:** 화면에 “점 1개는 많은 분자를 나타내는 대표입자”라고 명시한다. n을 0.25 mol 간격으로 제한해 10개씩 증감시키거나, 점 1개=0.025 mol로 제한하고 표시 소수 자릿수를 맞춘다. 충격량 측정을 유지할 경우 속도에는 분자 질량, 충격량 합에는 대표 가중치를 별도로 사용한다.

- `[중대]` 압력 계기의 충돌 집계 대상이 없어 보일의 법칙 설명이 구현에 따라 틀려진다.

  근거: `:273–274`는 부피를 반으로 하면 충돌 빈도가 두 배라고 단정한다. 이는 피스톤과 바닥 사이 왕복 충돌 또는 피스톤 충돌만 셀 때의 설명이다. 좌우 벽까지 포함한 “전체 벽 충돌 횟수”는 실린더 폭이 고정이므로 정확히 두 배가 되지 않는다. `:69–74`에는 어느 벽의 충격량인지 정의가 없다.

  **구체적인 수정 제안:** 압력은 피스톤 면에 전달된 수직 충격량만으로 정의하고, 계기 명칭도 `피스톤 충돌 횟수/s`와 `피스톤 평균 충격량`으로 바꾼다. 좌우 벽 플래시는 시각효과로만 두고 압력 집계에서는 제외한다.

- `[중대]` 절대영도 설명이 외삽 결과와 실제 물리 상태를 혼동한다.

  근거: `:99–102`의 축 해석 자체보다 `:296`의 “이 온도에서 입자 운동이 멈춘다고 외삽한 값”이 문제다. V–℃ 직선의 x절편은 이상기체 식을 외삽한 결과이며 실제 기체의 부피가 0이 되거나 모든 미시적 운동이 문자 그대로 멈추는 관측값이 아니다. 실제 기체는 먼저 응축하며 절대영도는 도달할 수 없다.

  **구체적인 수정 제안:** 피드백을 “이상기체의 V–T 직선을 외삽하면 부피가 0이 되는 온도인 −273.15 ℃에 도달한다. 실제 기체는 그 전에 액화하므로 이 직선은 그대로 적용되지 않는다”로 바꾼다. 입자 애니메이션도 0 K 정지를 직접 보여 주지 않는다.

- `[중대]` 맥스웰 분포와 단열 과정은 성취기준의 필수 학습 내용이 아니며 현재는 장식이 아니라 게이트·핵심 인과에 침투해 있다.

  근거: 2022 개정 교육과정 `[12물에01-01]`은 P·V·T·n의 통합 관계와 이상기체 방정식의 근사적 활용을 요구한다. 맥스웰 속력분포, 피스톤 일, 단열식은 명시된 범위를 벗어난다. 기획서도 `:499`에서 단열 압축이 범위 밖일 가능성을 인정한다. [교육부 고시 제2022-33호 과학과 교육과정](https://www.moe.go.kr/boardCnts/viewRenew.do?boardID=141&boardSeq=93458&lev=0)

  **구체적인 수정 제안:** 학생 화면에서는 “온도가 높을수록 평균 속력이 커진다”까지만 사용한다. 맥스웰 분포명·수식과 단열 압축은 교사용 설명 또는 선택형 `더 알아보기`로 이동하고 진도 통과 조건과 평가에서는 제거한다.

- `[중대]` 1차시 운영은 안정적으로 끝나지 않는다. 다만 실제 요구량은 “6단계×4점=24점”이 아니라 3개 탐구×4점=12점이다.

  근거: 데이터 4점 조건은 ③, ④, ⑤에만 있다(`:279`, `:299`, `:316`). 그래도 예상 3문항, 12개 상태 조작·정착·기록, 축 전환·절편 판독, 서술 5개 이상, 통합표, 형성평가 3개, AI 호출, 내보내기까지 포함한다. 도입·예상 7–10분, 탐구별 8–10분, 통합·평가 10분만 잡아도 41–50분이며 기기 지연과 재시도 시간이 없다.

  **구체적인 수정 제안:** 기본안을 2차시로 나눠 1차시는 ①–④, 2차시는 ⑤–⑥으로 한다. 반드시 50분이면 각 법칙을 3점으로 줄이고 기준점 1개는 미리 제공한다. 형성평가는 1문항만 수업 중 받고 나머지는 후속 과제로 돌리며 AI 검수는 비동기·비게이팅으로 처리한다. 대안은 모둠별 한 법칙만 4점 수집하고 다른 두 법칙은 제공 데이터로 합치는 방식이다.

- `[중대]` 게이팅은 학습 성취보다 UI 의식 수행을 강제하면서도 동일한 점 네 번 찍는 편법은 막지 못한다.

  근거: `:356–359`는 1/V 토글, K축 토글, 절편 정답, 내보내기까지 강제한다. 반면 데이터 점의 고유성·간격·범위는 명세하지 않아 같은 상태를 네 번 기록해도 통과할 수 있다. “정착” 기준도 피스톤 위치인지 압력 노이즈인지 정의되지 않았다. 음수 기호 `−`, `-` 입력 차이도 처리 규칙이 없다.

  **구체적인 수정 제안:** 데이터 게이트를 “서로 다른 제어값 3개 이상 + 최소 범위 충족”으로 변경한다. 예: 보일 P 최솟값/최댓값 비율 ≥2, 샤를 T 범위 ≥150 K. 동일 설정 중복 기록을 막고, 정착은 `|V−V_eq|/V_eq<0.5%` 및 피스톤 속도 임계값으로만 판단한다. 축 토글과 내보내기는 통과 조건에서 제거하고, 절편 입력은 유니코드 마이너스를 정규화한다. 학생용 `잠금 건너뛰기—미완료로 기록`과 교사용 명시적 override도 둔다.

- `[중대]` 단일 localStorage 키는 공용 학교 기기에서 학생 간 결과를 섞고 노출한다.

  근거: `:147–172`는 origin 전체에서 `ideal_gas_v1` 하나만 사용한다. 같은 태블릿에서 다음 학생이 로그인하면 앞 학생의 `unlockedStep`, 응답, 평가가 그대로 복원된다. 관리자 미리보기와 학생 창, iframe과 blob 새 탭을 동시에 열면 같은 키를 두 창이 덮어쓸 수도 있다.

  **구체적인 수정 제안:** 키를 `ideal_gas_v1:${sid}`로 분리하고, 저장된 sid와 현재 `__SITE_STUDENT__.sid`가 다르면 자동 복원하지 않는다. sid가 없는 직접 실행은 `ideal_gas_v1:guest:<uuid>`를 사용한다. 관리자 키도 별도로 분리하고, `updatedAt` 기반 충돌 감지 또는 `BroadcastChannel`로 다중 탭 덮어쓰기를 막는다.

- `[중대]` 실제 iframe 높이 안에 제안 레이아웃이 들어가지 않는다.

  근거: 실제 코드는 `단원-상세.html:204`에서 `height:min(80vh,760px)`이다. 높이 768px 화면에서는 약 614px뿐이다. 내부 고정 진행바, 단계 제목, 420px 캔버스, 패딩, 컨트롤만으로 이미 대부분을 소모한다. 720px 이하에서 캔버스→계기판→그래프를 1열로 쌓으면 `:138–141`의 “캔버스와 조작을 동시에 본다”는 목표를 달성할 수 없다. 컨트롤만 sticky여도 캔버스는 위로 사라진다.

  **구체적인 수정 제안:** 인라인 모드에서는 현재 단계의 `캔버스+핵심 계기+컨트롤`을 400–440px짜리 sticky 실험 트레이 하나로 만들고 표·질문은 아래로 분리한다. 모바일 캔버스 높이는 240–280px로 줄인다. 또는 부모와 `postMessage`로 높이를 동적 조정하도록 `단원-상세.html`까지 수정한다. 수정하지 않는다면 아코디언에는 요약만 두고 `새 탭에서 크게 보기`를 기본 진입으로 지정한다.

- `[중대]` same-origin `srcdoc` 구조는 저장 편의 대신 업로드 HTML에 부모 사이트 전체 권한을 준다.

  근거: 실제 iframe에는 `sandbox`가 없다(`단원-상세.html:204`). 받은 HTML을 그대로 `srcdoc`에 넣는다(`:219–224`). 따라서 HTML 자료는 부모 DOM, 같은 origin의 localStorage와 인증 관련 저장소에 접근할 수 있다. blob 새 탭도 creator origin을 상속하며 `window.opener`도 남아 있다(`:230–237`). 저장소 HTML이 변조되면 수업 사이트 XSS가 된다.

  **구체적인 수정 제안:** HTML 자료를 별도 origin에서 제공하거나 iframe에 `sandbox="allow-scripts allow-downloads"`를 적용하고 `allow-same-origin`은 빼야 한다. 학생 정보와 저장은 `postMessage` 기반 부모 저장 브리지로 전달한다. 새 탭도 별도 origin URL을 사용하거나 최소한 `w.opener=null`을 설정한다. 이 구조를 유지한다면 HTML 업로드 권한을 신뢰된 관리자에게만 제한하고 저장 파일 무결성 검사를 추가해야 한다.

- `[중대]` Edge Function에 대한 §8.4 진단은 맞지만 `context` 문자열 추가만으로는 안전하거나 충분한 수정이 아니다.

  근거: 실제 `SYSTEM_TEXT`는 스펙트럼으로 하드코딩되어 있다(`index.ts:49–57`), 12개 제한도 실제로 존재한다(`:86`). 그러나 사진 지침 역시 빛·스펙트럼 전용이다(`:53`). 임의의 클라이언트 `context`를 system prompt에 삽입하면 prompt injection 통로가 된다. 또한 CORS `*`이고(`:20–24`) 함수 자체에는 인증·호출량 제한이 없으며(`:81–90`), 기존 HTML도 공개 apikey만 보낸다(`스펙트럼_실험_페이지.html:590`). 외부에서 Gemini 할당량을 소모시킬 수 있다.

  **구체적인 수정 제안:** body에는 자유문자열 `context` 대신 `labId:"ideal-gas-v1"`만 받고 서버 allowlist에서 고정 프롬프트를 선택한다. `kind`, key, text 길이, 총 body 크기를 검증하고 IP/학생별 rate limit를 둔다. 가능하면 Supabase JWT 또는 부모가 발급한 단기 서명 토큰을 요구한다. 이 수업에서는 AI가 과학 정확성을 평가하지도 않으므로, 50분 수업 기본 설정은 `AI_CHECK:false`가 더 일관적이다.

- `[중대]` 기존 스펙트럼 구현을 근거로 든 교사 bypass와 저장 관례가 실제 파일과 다르다.

  근거: 기획서 `:181`, `:362`는 커밋 `875e002`를 근거로 교사가 “모든 게이팅 통과”한다고 한다. 실제 그 커밋은 사진 없이 ④만 통과시키는 변경이다. 현재 구현도 관리자에게 사진과 AI만 면제한다(`스펙트럼_실험_페이지.html:497–505`). ② 정답 게이트와 ③ 예상 게이트는 그대로다(`:530–538`). 또한 실제 구현에는 `localStorage` 호출이 하나도 없고, ⑥ 저장·JSON은 아직 스텁이다(`:484–490`). 따라서 이를 완성된 저장·복원 관례로 인용할 수 없다.

  **구체적인 수정 제안:** 기획서에서 “스펙트럼과 동일한 정책”과 해당 커밋 인용을 삭제하고 이상기체 페이지 자체의 `isAdmin` 분기를 명세한다. 저장·복원·내보내기는 신규 기능으로 분류하고 새로고침, 학생 변경, 다중 탭, quota 실패에 대한 테스트 항목을 제작 순서에 추가한다.

- `[중대]` 상태 스키마가 화면 요구사항과 맞지 않아 구현 중 필드가 즉석 추가될 수밖에 없다.

  근거: ②는 예상 3개와 이유 서술을 요구하지만(`:234–247`) 스키마의 `predict`에는 `pv/vt/pt`만 있고 이유 필드가 없다(`:159`). 보일 단계는 1/V 토글 사용을 게이트로 요구하지만 전용 필드가 불명확하다. 기록한 P가 설정압력인지 충돌압력인지, 측정창·분산·표본 수를 저장하는 필드도 없다. 중복점 판정, 평형 상태, 프린트용 그래프 스냅샷, AI 호출 상태의 복원 규칙도 빠져 있다.

  **구체적인 수정 제안:** 구현 전에 스키마를 `predict.reason`, 각 점의 `{setpoint,theory,measured,stderr,sampleCount,recordedAt}`, `boyle.inverseViewUsed`, `settled`, `exportedAt`, `schemaVersion`까지 확정한다. v1 데이터 migration 함수와 손상 JSON fallback도 명세한다.

- `[경미]` 저장소 경로와 줄번호는 현재 맞지만 §9.2의 두 동작 설명은 부정확하다.

  근거: `MATERIAL_TYPES.html`은 실제 `js/db.js:141`, `materialHtmlCard`는 `단원-상세.html:186`, iframe 높이는 `:204`, `loadHtmlIntoIframe`은 `:219`, `openHtmlMaterial`은 `:230`이다. 색상값도 실제 `js/theme.js:22–28`과 §7 표가 일치한다. 틀린 부분은 다음 두 가지다.

  - `injectStudentInfo`는 `<head>`가 없으면 실패하는 것이 아니라 문서 맨 앞에 스크립트를 붙인다(`단원-상세.html:217`). 다만 이 경우 스크립트가 doctype보다 앞서 standards mode를 깨뜨릴 수 있다.
  - §3.1은 주 키를 `ideal_gas_v1`이라고 하지만 §9.2는 전 키를 `ideal_gas_v1_`로 쓰라고 하여 서로 충돌한다(`:147–151`, `:464`).

  **구체적인 수정 제안:** `<head>` 요구 사유를 “주입 불가”가 아니라 “doctype 유지와 안정적인 주입 위치 보장”으로 고친다. 키 규칙은 주 키 `ideal_gas_v1:${sid}`, 보조 키 `ideal_gas_v1:${sid}:*`로 한 문장에 통일한다.

- `[경미]` `srcdoc`과 blob의 localStorage 설명은 HTTPS 배포에서 맞지만 직접 파일 실행까지 일반화하면 안 된다.

  근거: 현재 iframe은 sandbox가 없으므로 `about:srcdoc` 문서는 부모 origin을 상속한다. HTTP(S) 페이지에서 만든 blob URL도 creator origin을 가지므로 새 탭 역시 같은 localStorage를 본다. 따라서 “blob라서 별도 저장소가 된다”는 주장은 틀리다. 다만 `file:` URL의 localStorage 동작은 브라우저별로 보장되지 않는다. [MDN `srcdoc`](https://developer.mozilla.org/en-US/docs/Web/API/HTMLIFrameElement/srcdoc), [MDN blob URL](https://developer.mozilla.org/en-US/docs/Web/URI/Reference/Schemes/blob), [MDN localStorage](https://developer.mozilla.org/en-US/docs/Web/API/Window/localStorage)

  **구체적인 수정 제안:** §3.1에 “수업 사이트의 HTTP(S) origin에서 열 때”라는 조건을 추가한다. 직접 파일 실행 fallback에서는 localStorage 사용 가능 여부를 `try/catch`로 검사하고 메모리 저장+JSON 내보내기로 전환한다. `openHtmlMaterial`은 생성한 blob URL을 새 탭 로드 후 적절한 시점에 revoke하도록 수정한다.

- `[경미]` IntersectionObserver는 유용하지만 “없으면 태블릿이 죽는다”와 “반드시 필요”는 과장이고, 더 단순한 생명주기 설계가 빠졌다.

  근거: 입자 간 충돌이 없고 최대 5×80=400개 입자를 한 루프에서 갱신하므로 계산량 자체는 작다(`:408–413`). 문제는 완료된 시뮬레이션을 계속 렌더링하는 구조다. IntersectionObserver만으로는 숨겨진 탭의 누적 시간이나 다중 iframe 상태까지 완전히 처리하지 못한다.

  **구체적인 수정 제안:** 현재 단계의 엔진 하나만 실행하고, 완료 단계 캔버스는 정지된 스냅샷으로 남긴다. IntersectionObserver는 보조 수단으로 사용하고 `document.visibilitychange`, 아코디언 닫힘 메시지, 프레임 복귀 시 누적 `dt` 상한과 최대 물리 스텝 수를 추가한다.

- `[경미]` 스펙트럼 구현은 “오프라인 단일 HTML”의 실제 기준 구현이 아니다.

  근거: 스펙트럼 기획서는 오프라인·외부 라이브러리 없음이라고 쓰지만(`스펙트럼_관찰_실험_페이지_기획서_v1.6.md:4,14`), 실제 HTML은 Pretendard와 Google Fonts CDN을 로드한다(`스펙트럼_실험_페이지.html:7–10`). 이상기체 기획서의 CDN 0 방침과 다르다.

  **구체적인 수정 제안:** 스펙트럼 파일은 화면 흐름 참고자료로만 명시하고 자립형 구현 기준으로 인용하지 않는다. 이상기체 파일에는 실제 네트워크 차단 상태에서 폰트·아이콘·AI 실패·내보내기가 동작하는 테스트를 별도로 둔다.

### 판정

**재설계 필요** — 압력 차원·피스톤 일·평형 이완이라는 핵심 물리 엔진이 정의되지 않았고, 현재 구조로는 “측정 데이터에서 PV=nRT를 발견한다”는 수업의 중심 주장이 성립하지 않는다.
