# 이상기체 상태 방정식 기획서 플랜 리뷰 — Round 13

- 대상: `실험 수업/2학기/물질과에너지/Ⅰ. 물질의 세가지 상태/1. 기체/이상기체 상태 방정식.md` v1.13
- 검토본 SHA-256: `1E3B0D1CF9608CBBA69FC7555367D49AE77294C69DCA6D825AD9BE6CEE0A585F`
- 결론: **남은 결함 없음 — critical 0, major 0, minor 0, nit 0**
- 원문과 리포의 다른 파일은 수정하지 않았다.

## Round 12 지적 5건 반영 검증

| R12 항목 | v1.13 검증 결과 |
|---|---|
| 1. AI 판정 정책 충돌 | **해소.** §8.4가 `ideal_gas`에도 공통 “과학적 정확성은 채점하지 않고 관련성·성의만 판정” 정책을 적용한다고 고정했고, 8개 expect를 정답 요건이 아닌 질문별 관련성 맥락으로 바꿨다. `assess.a3`의 낱말 요건은 클라이언트 로컬 검사로 분리되어 §5⑥·§6·§8.4에 동일하게 반영됐다. |
| 2. 이산 적분 정착 시간 | **해소.** §1.2의 잔존 `≈0.6 s` 표현이 없어졌고 §1.3은 연속 폐형식 참고치와 실제 이산 적분 기대값을 분리했다. 명시된 velocity-first 세미-임플리시트 오일러를 직접 반복한 결과 `A=1`은 86 step = 0.716666… s, `A=11`은 106 step = 0.883333… s로 문서의 0.72/0.88 s와 일치했다. |
| 3. 화면 밖 복귀 시 누산기 순서 | **해소.** §8.2가 정착 복귀와 이동 중 복귀를 분기한다. 전자는 복귀 즉시 새 2 s 창을 시작하고, 후자는 보존 상태로 ODE를 재개한 뒤 §1.3의 `정착 → 스냅 → 누산기 재초기화 → 2 s 완충` 순서를 따르므로 §1.2와 모순이 없다. |
| 4. 사진 wire 필드·미디어 타입 | **해소.** §8.4의 개별 상한 대상이 현행 wire 필드 `imageBase64` 문자열로 고쳐졌고 `mediaType` 허용목록 및 실제 스펙트럼 payload 타입을 포함시키는 회귀 기준이 명시됐다. `contextId` 미전송의 spectrum 폴백, key·kind·중복·크기·본문 상한 검증도 그대로 유지된다. |
| 5. T 끝점 도달 불가 | **해소.** 범위를 넘는 다음 조작만 100/600 K로 스냅하고 끝점 바깥 방향을 비활성화하도록 확정됐다. 273 K에서 실제 도달 집합은 기존 `273+25k` 계열에 끝점과 끝점에서 되돌아온 `100+25k` 계열이 합쳐져 최솟값 100 K, 최댓값 600 K가 된다. |

## 수치·회귀 확인

### 피스톤 정착

검산에 사용한 이산식은 다음과 같다.

`vₖ₊₁ = vₖ + dt·(−ω²xₖ − 2ωvₖ)`, `xₖ₊₁ = xₖ + dt·vₖ₊₁`

`ω=20 s⁻¹`, `dt=1/120 s`, 정착 조건 `|x|<10⁻⁴` 및 `|v|<10⁻³ s⁻¹`에서:

| 초기 상대 진폭 | 최초 충족 step | 시간 | 그때 `x` | 그때 `v` |
|---:|---:|---:|---:|---:|
| `A=1` | 86 | 0.716666… s | `6.38268×10⁻⁵` | `−9.57402×10⁻⁴ s⁻¹` |
| `A=11` | 106 | 0.883333… s | `6.65802×10⁻⁵` | `−9.98703×10⁻⁴ s⁻¹` |

따라서 §1.3의 step 수와 반올림 시간은 정확하다. 이동 중 재조작은 시간 하드코딩 대신 동일한 두 수치 조건으로 판정하고 `V_eq`만 바꾸므로 §1.2의 평형 판정·§8.2 복귀 정책과도 정합한다.

### 온도 스냅·게이트

- 273 K에서 냉각하면 `… 173, 148, 123, 100 K`, 가열하면 `… 548, 573, 598, 600 K`가 된다.
- 끝점에서 반대 방향으로 돌아오면 25 K 간격 계열도 정상 유지된다. 전체 도달 집합은 41개 값이고 모두 `[100,600]` 안에 있다.
- ④·⑤의 폭 150 K 게이트는 예를 들어 `273 → 298 → … → 423 K`의 한 방향 6회로 그대로 충족 가능하다.
- 검산값도 `P=1 atm` 등압에서 `V(100)=8.21 L`, `V(600)=49.26 L`; `V=22.4 L` 등적에서 `P(100)=0.3665 atm`, `P(600)=2.1991 atm`으로 §1.4의 V·반응압력 범위 안이다. 스냅 후보가 이 범위를 위반하는 경우에는 §1.4의 기존 V·`P_이론` 사전 검사로 차단하므로 조용한 클램프가 생기지 않는다.

### AI·저장·내보내기

- `assess.a3`는 먼저 trim 및 `빈도` + (`세기` 또는 `충격`) 로컬 검사를 통과해야 ⑥의 나머지 조건과 함께 내보내기 시도가 가능하다.
- AI는 별도로 현재 원문 텍스트의 해시가 붙은 관련성 판정만 저장한다. 입력 수정 시 해당 판정 삭제·진행 요청 취소 규칙이 적용된다.
- `AI_CHECK=true`인 내보내기는 모든 현재 대상이 종결된 뒤 실행되고, `REQUIRE_AI_PASS`는 `fail` 차단 여부만 바꾼다. 답·`aiCheck`가 바뀌면 둘 다 내보내기 정규 payload에 포함되므로 `exportedHash`가 불일치하여 완료가 즉시 풀린다.
- 로드 시 ②~⑤는 `REQUIRE_AI_PASS=true`일 때 현재 텍스트 해시와 일치하는 `ok/unavailable`만 충족으로 인정한다. ⑥은 로컬 a3 검사와 현재 payload의 내보내기 해시 일치가 완료 조건이므로 새 우회나 교착 경로가 생기지 않는다.

### 기타 회귀

- §1.2의 해석식 `F=N·m·⟨v_y²⟩/H`, `P=F/W`와 보정 기준에는 변화나 새 충돌이 없다.
- §2.1 캐스케이드는 360×640에서 ③(1열), 640×360에서 ④(저높이 2열), 800×360에서 ④, 800×600에서 ②(축소 2열), 1024×768에서 기본 2열이 적용된다. §8.3 체크리스트와 일치하며 경계 공백도 없다.
- §8.4의 `ideal_gas` 8개 key, spectrum 6개 하위 호환 key, 서버 고정 expect, 중복 거부, kind 제한, 12개/2,000자/7,000,000자/20 MB 상한, 120회/분 제한, 응답 대조 규칙은 새 관련성 정책 이후에도 서로 충돌하지 않는다.

## 남은 발견 사항

**이상 없음.**
