# 이상기체 상태 방정식 기획서 플랜 리뷰 — Round 11

- 대상: `실험 수업/2학기/물질과에너지/Ⅰ. 물질의 세가지 상태/1. 기체/이상기체 상태 방정식.md` v1.11
- 결론: **결함 8건 — critical 0, major 5, minor 3, nit 0**
- 원문은 수정하지 않았다.

## 발견 사항

### 1. [major] §1.3 — “일반 응답” 식과 정착 시간 상한은 조작 시작 순간의 피스톤 속도가 0일 때만 성립한다

문서의

`|V−V_eq|/V_eq = A(1+t/τ)e^(−t/τ)`

는 임계감쇠의 일반해가 아니라, 새 목표가 설정될 때 `dV/dt=0`인 특수한 초기조건의 해다. 임계감쇠 일반해는 `x(t)=(C₁+C₂t)e^(−t/τ)`이고 `C₂=v₀+x₀/τ`이므로, 이동 중 학생이 온도·압력·n 버튼을 다시 눌러 목표 `V_eq`를 바꾸면 현재의 비영 속도 `v₀` 항이 남는다.

그런데 §1.2·§5는 이동 중 `기록`만 막을 뿐 조작 컨트롤을 막거나, 새 조작 시 피스톤 속도를 0으로 리셋하거나, 조작을 큐잉한다고 정하지 않았다. 따라서 두 정상 구현(연속 조작 허용/정착까지 조작 금지)이 서로 다른 궤적과 정착 시간을 만들며, 연속 조작 구현에서는 `A≤11 → 약 0.75 s` 상한도 보장되지 않는다.

수치 자체는 해당 특수 초기조건에서는 맞다. 두 정착 조건을 동시에 처음 만족하는 시각은 `A=1`에서 `12.423τ = 0.6212 s`, `A=11`에서 `15.010τ = 0.7505 s`다. 즉 문서의 약 `13τ`, 약 `15τ`는 반올림으로 타당하지만, 이를 적용할 초기속도/연속 조작 정책을 확정해야 한다.

### 2. [major] §3.1·§6·§8.4 — `REQUIRE_AI_PASS=true`의 중간 단계 차단을 새로고침으로 우회할 수 있다

§6의 AI 규칙은 ②~⑤에서 **`다음` 버튼 클릭 시** `fail`을 차단한다고만 정한다. 반면 §3.1의 로드 재평가는 §6 표의 필드로 ①부터 순차 재평가하며, 그 표의 조건과 “판정에 쓰는 저장 필드”에는 `aiCheck`가 없다.

따라서 내용 게이트를 모두 채운 뒤 AI가 `fail`하여 다음 버튼이 막힌 학생이 새로고침하면, 로드 재평가는 내용 조건만 보고 다음 단계를 열 수 있다. AI 요청이 아직 진행 중이거나 클릭 전에 새로고침한 경우도 같다. 이는 `unlockedStep`을 불신해 우회를 막는다는 §3.1의 목적과 충돌한다.

로드 재평가에도 `AI_CHECK && REQUIRE_AI_PASS`일 때의 종결 상태와 현재 답변 해시를 포함할지, 아니면 별도의 “AI 게이트 통과” 증거를 저장할지를 명시해야 한다. `fail`·미검수·낡은 해시의 처리가 §6 표와 재평가 알고리즘에 들어가야 한다.

### 3. [major] §3.1·§3.2 — `_anon`에서 학번 키로 이관할 때 활성 키와 내장 학생 식별자를 어떻게 바꾸는지 빠져 있다

§3.2는 저장 키를 “로드 시 1회” 결정한다고 한 뒤, 수동 학번 입력 시 anon JSON을 대상 키로 복사하고 anon 키를 삭제한다고만 한다. 그러나 복사본의 `student.sid`는 여전히 빈 문자열이고, 이후 자동저장이 사용할 활성 키를 `_<학번>`으로 전환한다는 규칙도 없다.

문구대로 구현하면 다음 중 하나가 발생한다.

- 복사 직후 자동저장이 계속 `_anon`을 다시 만들어 데이터가 두 키로 갈라진다.
- 대상 키에는 `student.sid=""`인 복사본이 남아, 이후 §3.1의 SID 불일치 검증에 걸려 복원되지 않는다.
- 대상 키가 이미 있을 때의 “이어하기 / 새로 시작” 뒤 어떤 키를 활성화하고 anon 데이터를 보존·삭제하는지, 대상 데이터를 로드 검증·게이트 재평가하는지가 구현마다 달라진다.

이관 트랜잭션의 순서(대상 검증 → `state.student.sid/name` 갱신 → 대상 저장 성공 확인 → 활성 키 교체 → anon 삭제)와 두 선택지의 정확한 데이터 처리 규칙이 필요하다.

### 4. [major] §8.4·§5 — `ideal_gas`의 서버측 `expect` 매핑 내용이 정의되어 있지 않다

§8.4는 클라이언트가 보낸 `expect`를 무시하고 서버가 `contextId+key`로 선택하도록 강제한다. `spectrum`은 현행 6개 expect 문구를 그대로 옮기라고 명시하지만, `ideal_gas`는 8개 key 이름만 열거하고 각 key의 서버측 expect 문구를 제공하지 않는다.

따라서 이 문서만으로는 `predict.reason`, `boyle.q1`, `boyle.q2`, `charles.relation`, `gaylussac.relation`, `assess.a1~a3`의 판정 프롬프트를 구현할 수 없다. 특히 `predict.reason`은 세 예상을 함께 설명하는 자유 서술이고, `assess.a3`은 “충돌 빈도·세기”라는 필수 낱말이 있어 구현자가 단순 관련성 검사 또는 내용 요구 검사 중 무엇을 써야 하는지가 갈린다.

클라이언트 자유 문자열을 금지한 설계를 유지하려면 8개 key 각각의 정확한 서버측 expect 문자열을 기획서에 고정해야 한다.

### 5. [major] §10 — 앙상블 신뢰구간 판정 절차가 통계적으로 완결되지 않았다

`√(4/(πN))`은 가우시안 법선속도에서 충돌 입자의 flux-weighted 충격량과 Poisson 충돌수를 합친 compound-Poisson 근사로 재계산할 때 맞다. `N=2,050`이면 상대 표준편차 `0.0249217`, `2σ=0.0498435`로 문서의 약 4.98%도 맞다.

그러나 실제 합격 판정은 다음이 정해지지 않아 서로 다른 결과가 나온다.

- `N≥2,050`이 **각 P·V·T·n 조합마다**인지, 6개 이상 조합 전체의 합인지 명시가 없다. 전체 합이면 조합당 약 342회뿐일 수 있어 `2σ≈12.2%`다.
- 같은 seed의 비중첩 시간창은 이 엔진의 입자별 주기 충돌 때문에 독립 표본이 아니다. 창들을 독립 반복으로 세어 표준오차를 줄이면 신뢰구간이 과도하게 좁아진다. seed별 집계를 독립 단위로 삼거나 상관을 보존하는 블록 방법이 필요하다.
- seed 수는 “예: 8개”일 뿐 고정값이 아니며, 신뢰수준·CI 공식·통과 규칙이 없다. 8개의 독립 seed 평균에 95% t-구간을 쓰면 임계값은 `2`가 아니라 자유도 7의 약 `2.365`다.
- “`1.00±5%`”와 “신뢰구간으로 판정”의 결합도 불명확하다. CI가 1을 포함하면 통과인지, 점추정치가 범위 안이면 되는지, CI 전체가 `[0.95,1.05]` 안에 들어야 하는지에 따라 결론이 다르다.

문서가 독립 Poisson 가정 불일치를 인지하고 실측 분산을 요구한 것은 맞지만, 위 네 항목을 고정하지 않으면 검증 절차 자체가 재현되지 않는다.

### 6. [minor] §2.1·§8.3·§12 — 비중첩 미디어쿼리가 CSS의 연속 길이에서 1 px 공백 구간을 만든다

CSS 미디어 길이는 정수가 아니므로 다음 규칙은 비중첩이지만 전 구간을 덮지 않는다.

- `max-height:470px`와 `min-height:471px` 사이의 `470px < height < 471px`
- `max-width:720px`와 `min-width:721px` 사이의 `720px < width < 721px`

예를 들어 부모 높이 `588.125px`이면 iframe의 `80vh`가 `470.5px`가 되어 저높이 규칙과 471px 이상 규칙 모두 적용되지 않는다. 폭 `720.5px`, 내부 높이 `480px`에서도 1열 규칙과 폭>720 저높이 축소 규칙이 모두 빠진다. 브라우저 줌, 퍼센트 레이아웃, DPR 변환으로 이런 소수 CSS px는 실제로 생긴다.

요청된 정수 예시는 모두 의도한 분기로 들어간다.

| 부모 뷰포트 | iframe 내부 높이 | 적용 규칙 |
|---|---:|---|
| 360×640 | 512px | 폭≤720·높이≥471의 1열 |
| 640×360 | 288px | 높이≤470의 저높이 2열 |
| 800×360 | 288px | 높이≤470의 저높이 2열 |
| 800×600 | 480px | 폭>720·높이471~530 축소 |
| 1024×768 | 614.4px | 기본 2열 |

정수 예시 통과만으로 “전 조합”은 성립하지 않는다. 한 규칙을 기본값으로 두고 반대쪽만 override하는 등 경계 공백이 없는 방식으로 정의해야 한다.

### 7. [minor] §8.4 — 서버 방어 한도가 코드로 옮길 수 있는 확정값이 아니며 스펙트럼 하위 호환 시험 범위도 부족하다

항목당 텍스트 상한은 “예: 2,000자”, 요청 본문 크기 상한은 값 없음, IP 제한은 “수업 버스트를 수용하는 넉넉한 한도”로만 되어 있다. 이 값들은 보안·비용뿐 아니라 기존 스펙트럼 호출의 성공 여부를 바꾼다.

실제 `스펙트럼_실험_페이지.html`은 파일을 `FileReader.readAsDataURL`로 그대로 읽고(542~549행), 리사이즈 없이 최대 4개 사진의 base64를 한 요청에 넣는다(579~590행). 따라서 구현자가 임의로 낮은 본문 상한을 정하면 “`contextId` 미전송” 회귀 테스트는 통과하면서 실제 사진 요청은 400으로 회귀할 수 있다.

확정 본문/텍스트/사진별 크기와 rate-limit 수치, 그리고 기존 형식의 대표적인 4사진 payload가 통과하는 회귀 기준이 필요하다.

### 8. [minor] §1.4·§5④⑤·§8.3 — 온도 조작의 UI 형태와 증감 간격이 정의되어 있지 않다

압력은 추 1개당 `0.5 atm`, n은 `0.25 mol`로 고정했지만 온도는 범위 `100~600 K`만 있고 증감 간격이 없다. §5의 화면은 `가열/냉각` 버튼처럼 보이는 반면 §8.3은 “슬라이더·추 버튼”이라고 하므로, 1 K 연속 슬라이더·10/25/50 K 버튼 등 서로 다른 구현이 모두 문서와 양립한다.

이는 단순 표시 취향이 아니다. ④⑤의 “서로 다른 점 4개 + 폭 150 K”를 충족하는 조작 횟수, 키보드 화살표 한 번의 변화량, 정착/2 s 워밍업 반복 횟수가 달라진다. T 컨트롤 종류와 `step`을 확정해야 한다.

## 재계산 검증 요약

- §1.2 해석식: 한 입자의 피스톤 충돌 빈도 `|v_y|/(2H)` × 충격량 `2m|v_y|`이므로 `F=N·m·⟨v_y²⟩/H`, 2D 단위 길이 압력 `P=F/W`는 맞다.
- §1.3: 정지 상태에서 목표를 한 번 바꾸는 조건에서는 `A=1`의 정착 `0.6212 s`, `A=11`의 정착 `0.7505 s`로 반올림 문구가 맞다. 발견 1은 초기속도 전제가 없는 문제다.
- §1.4: `R=0.0821`이면 `RT`는 273 K에서 `22.4133`, 100 K에서 `8.21`, 600 K에서 `49.26 L·atm/mol`이므로 표의 검산값은 표시 반올림 범위에서 맞다.
- §10: Poisson 근사의 `√(4/(πN))`과 `N=2,050 → 2σ≈4.984%`는 맞다. 발견 5는 실제 비독립 엔진에서 CI를 만드는 절차와 합격 규칙이 빠진 문제다.
