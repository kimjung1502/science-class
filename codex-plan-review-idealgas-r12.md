# 이상기체 상태 방정식 기획서 플랜 리뷰 — Round 12

- 대상: `실험 수업/2학기/물질과에너지/Ⅰ. 물질의 세가지 상태/1. 기체/이상기체 상태 방정식.md` v1.12
- 결론: **남은 결함 5건 — critical 0, major 1, minor 4, nit 0**
- 원문은 수정하지 않았다.

## Round 11 지적 8건 반영 검증

| R11 항목 | v1.12 검증 결과 |
|---|---|
| 1. 피스톤 재조작·임계감쇠 초기조건 | **핵심 결함 해소.** ODE, 적분 간격, 재조작 시 상태 연속, 폐형식의 적용 전제가 확정됐다. 다만 이산 적분의 실제 정착 시간과 §1.2의 잔존 문구가 맞지 않는 minor 1건이 남았다(발견 2). |
| 2. `REQUIRE_AI_PASS` 새로고침 우회 | **해소.** 현재 텍스트와 일치하는 `textHash` 및 `ok/unavailable`을 로드 재평가 조건에 포함하고, `fail`·미검수·해시 불일치에서 해당 단계를 편집 가능 상태로 멈추도록 §3.1과 §6이 일치한다. |
| 3. `_anon`→학번 이관 | **해소.** 대상 검증, SID/name 갱신, 저장 재확인, 활성 키 교체, anon 삭제의 순서와 실패 원자성, 기존 대상의 두 선택지 처리가 확정됐다. |
| 4. `ideal_gas` expect 매핑 누락 | **매핑 자체는 해소.** 8개 고정 문구가 생겼다. 다만 새 매핑의 정확성 판정과 상위 시스템 정책이 충돌하는 major 회귀가 있다(발견 1). |
| 5. §10 앙상블·CI 절차 | **해소.** N은 조합별, 독립 단위는 seed 8개, seed별 집계, `t₇=2.365`, 합격식과 측정 연장 조건이 모두 고정됐다. `√(4/(π·2050))=0.0249217`, `2σ=0.0498435`도 맞다. |
| 6. 미디어쿼리 소수 px 공백 | **해소.** 기본→`max-height:530`→`max-width:720`→`max-height:470` 순서와 후행 규칙의 전면 재지정이 명시되어 470.5px·720.5px도 빠지지 않는다. §8.3 체크리스트와 일치한다. |
| 7. 서버 상한·스펙트럼 회귀 기준 | **수치 미확정 문제는 해소.** 다만 현재 스펙트럼 wire field와 다른 `dataURL` 명칭이 남았다(발견 4). |
| 8. 온도 UI·step | **UI와 step은 해소.** 다만 273 K에서 ±25 K만 허용하면 문서가 선언한 100 K·600 K에 도달하지 못하는 새 범위 회귀가 있다(발견 5). |

## 남은 발견 사항

### 1. [major] §4·§8.4 — `ideal_gas` expect의 정답 채점과 상위 시스템 지침의 “과학적 정확성은 채점하지 않음”이 충돌한다

v1.12의 서버 expect는 `boyle.q1/q2`, `charles.relation`, `gaylussac.relation`, `assess.a1~a3`에 과학적으로 맞는 내용 요소를 요구하며, 특히 `assess.a3`은 빈도·세기 중 하나가 없으면 **실패**라고 명시한다. 반면 현재 Edge Function의 더 높은 우선순위인 `SYSTEM_TEXT`는 “과학적으로 정확한지는 채점하지 마 — 실험과 관련 있고 성의 있게 시도한 흔적이 있으면 통과”라고 명시하고 있고, 기획서도 이 검수를 “1차 필터”로 설명한다.

§8.4는 `contextId`별 시스템 지침을 선택한다고만 할 뿐 `ideal_gas`에서 위 공통 정책 문장을 제거·대체할지, expect를 단순 관련성 힌트로만 쓸지 정하지 않았다. 예를 들어 `boyle.q1`에 “압력을 2배 하면 부피도 2배”라고 성의 있게 쓴 답은 SYSTEM_TEXT를 따르면 `ok=true`, 새 expect를 따르면 `ok=false`다. `REQUIRE_AI_PASS=true`에서는 이 차이가 실제 진행 차단으로 이어진다.

`ideal_gas`용 SYSTEM_TEXT의 정확한 정책을 고정해 “오답도 관련 있으면 통과” 또는 “expect의 필수 과학 요소가 없으면 실패” 중 하나를 선택해야 한다. 현재 문구로는 서버 구현과 Gemini 판정이 결정적이지 않다.

### 2. [minor] §1.2·§1.3 — 세미-임플리시트 오일러 구현의 실제 정착 시간은 남아 있는 `≈0.6 s` 및 폐형식 참고치보다 길다

v1.12가 정한 표준 velocity-first 세미-임플리시트 오일러

`vₙ₊₁ = vₙ + dt·(−ω²xₙ − 2ωvₙ)`, `xₙ₊₁ = xₙ + dt·vₙ₊₁`

를 `ω=20`, `dt=1/120`으로 직접 반복하면 이산 상태행렬의 고유값은 `0.888888…`, `0.75`로 안정하다. 그러나 §1.3의 두 정착 조건을 처음 만족하는 시각은 정지 단발 조작에서 다음과 같다.

- `A=1`: 86 step = **0.7167 s**
- `A=11`: 106 step = **0.8833 s**

§1.3이 `0.62/0.75 s`를 연속 폐형식의 참고값으로만 한정한 것은 옳지만, §1.2에는 여전히 피스톤 이동 완료를 `≈0.6 s, §1.3`이라고 쓴 잔존 문구가 있다. 실제 구현의 UX·테스트 기대값으로 읽으면 약 15~18% 짧다. §1.2를 “수치 정착 조건 충족 후”로만 쓰거나, 이산 적분 기준의 실측 참고 시간을 병기해야 한다.

### 3. [minor] §1.2·§1.3·§8.2 — 화면 밖에서 이동 중이던 피스톤의 복귀 시 누산기 재초기화 순서가 두 가지로 읽힌다

§8.2는 재교차 시 타임스탬프와 `P_측정` 창을 리셋하고 **복귀 후 2 s가 지나면 기록 재활성**한다고 적는다. 반면 §1.2·§1.3은 피스톤이 움직이는 경우 **정착 → 누산기 다시 리셋 → 새 2 s 창 완충 → 기록 활성** 순서를 강제한다.

피스톤 이동 중 화면 밖으로 나갔다가 돌아오면 물리 상태 `V`·`dV/dt`는 보존되므로 복귀 후에도 ODE 이동이 이어진다. §8.2를 문자 그대로 따르면 복귀 순간부터 센 2 s 창에 이동 중 충돌이 섞이고 2 s 시점에 기록이 열릴 수 있고, §1.3을 따르면 정착 시 두 번째로 비운 뒤 그때부터 2 s를 기다린다.

복귀 시에도 최종 순서를 “물리 재개 → 정착 대기 → 정착 순간 누산기 재초기화 → 2 s 완충”으로 명시해야 한다. 이미 정착된 상태로 복귀한 경우에만 복귀 순간부터 2 s를 세도록 분기하면 된다.

### 4. [minor] §8.4 — 사진 상한을 적용할 wire field 이름이 현행 스펙트럼 payload와 다르다

§8.4는 `photo` 항목의 **dataURL 문자열**을 7,000,000자로 제한한다고 쓴다. 그러나 현행 `스펙트럼_실험_페이지.html`은 `FileReader`의 data URL을 `dataUrlParts()`로 분해한 뒤 접두어를 제거하고 다음 형태로 전송한다.

`{ key, kind:"photo", imageBase64: parts.base64, mediaType: parts.mediaType, expect }`

현재 Edge Function의 `Item` 타입도 `imageBase64?: string`과 `mediaType?: string`이다. 구현자가 문서대로 `dataURL` 필드를 검사하면 모든 현행 사진을 누락 필드로 거부하거나 7,000,000자 개별 상한을 전혀 적용하지 않게 된다(전체 20 MB 상한만 남음).

상한 대상을 wire 명칭 그대로 **`imageBase64` 문자열 7,000,000자**로 고치고, `mediaType`은 별도 허용목록으로 검증한다고 명시해야 한다.

### 5. [minor] §1.4·§5④⑤ — 273 K에서 ±25 K 고정 스텝과 범위 초과 방향 비활성 규칙으로는 선언한 100 K·600 K에 도달할 수 없다

초기값 273 K에서 ±25 K만 허용하면 가능한 값은 `273 + 25k`다. §1.4의 “다음 조작 결과가 범위를 벗어나면 그 방향 버튼 비활성” 규칙을 적용하면:

- 냉각 최저 도달값: **123 K** (`98 K`가 되는 다음 버튼은 비활성)
- 가열 최고 도달값: **598 K** (`623 K`가 되는 다음 버튼은 비활성)

따라서 표의 조작 범위 `100~600 K`와 검산의 `T=100→600 K`는 UI에서 재현되지 않는다. 150 K 폭 게이트 자체는 6회 조작으로 충족하므로 교착은 없지만, 범위·검산과 실제 조작 가능 집합이 충돌한다.

경계에서 마지막 스텝만 정확히 100/600 K로 맞추도록 허용하거나, 초기값/step을 서로 합동인 값으로 바꾸거나, 문서의 실제 도달 범위를 123~598 K로 고정해야 한다.

## 추가 회귀 확인 결과

- §1.2 해석식 `F=N·m·⟨v_y²⟩/H`, `P=F/W`와 v1.12 수정 사이에는 새 충돌이 없다.
- §3.1 재평가와 §6 게이트 배선은 `AI_CHECK`·`REQUIRE_AI_PASS` 네 조합에서 정합하며, `unavailable` fail-open도 유지된다.
- §10의 기본 상태 예상 충돌수는 `8 seed × 25 s × 약 24회/s = 약 4,800회`로 조합별 2,050회 기준을 만족한다. 드문 조합은 문서대로 측정 연장이 필요하다.
- §2.1 캐스케이드는 360×640, 640×360, 800×360, 800×600, 1024×768 및 470.5px·720.5px 경계에서 규칙 공백이 없다.
