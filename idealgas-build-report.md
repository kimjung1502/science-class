# 이상기체 상태 방정식 실험 페이지 — 빌드 리포트

> 기준 사양: `실험 수업/2학기/물질과에너지/Ⅰ. 물질의 세가지 상태/1. 기체/이상기체 상태 방정식.md` **v1.13**
> (§8.4 본문 상한만 코디네이터 결정으로 **v1.14 의 32 MB** 를 따름 — §4.1)
> 작성일 2026-07-26 · 구현자: 워커 `term_6dfb3a6e`

## 0. 산출물

| 파일 | 내용 |
|---|---|
| `실험 수업/2학기/물질과에너지/Ⅰ. 물질의 세가지 상태/1. 기체/이상기체_실험_페이지.html` | 자립형 단일 HTML (125 KB, 외부 CDN·상대경로 자산 **0**, `<head>` 명시) |
| `supabase/functions/check-experiment-response/index.ts` | §8.4 (A)안 — `contextId` 허용목록 + 서버측 expect 매핑 + 방어 검증 (**코드만 수정, 배포 안 함**) |
| `tests-idealgas/load-engine.js` | 배포 HTML 에서 물리 엔진 블록을 잘라내 Node 로 로드 |
| `tests-idealgas/verify.js` | §10 검증 (결정론 / `P_측정` 통계 / 방향 / 피스톤 정착) |
| `tests-idealgas/edge-validate.test.mjs` | Edge Function 검증 27건 (하위 호환 회귀 포함) |

```
node tests-idealgas/verify.js              → ✅ 전체 합격
node tests-idealgas/edge-validate.test.mjs → 27 통과 / 0 실패
```

**테스트한 코드 = 배포되는 코드**: 두 테스트 모두 산출물 파일에서 마커
(`IDEALGAS ENGINE BEGIN/END`, `PURE VALIDATION BEGIN/END`) 사이를 **문자 그대로 잘라내** 실행한다.
별도 사본을 두지 않으므로 둘이 어긋날 수 없다.

---

## 1. §10 검증 결과 (전부 실측 수치)

### 1.1 엔진 상수 (§1.2 해석적 보정 — 측정 기반 보정 없음)

고정 논리 좌표 `320×420`, 실린더 내부 폭 `W = 260`, 기체 기둥 높이 `H = 6.1 · V`(논리단위/L).

```
2D 운동론:  F = N·m·⟨v_y²⟩ / H       (피스톤이 받는 시간평균 총 힘)
            P = F / W = N·m·⟨v_y²⟩ / (W·H)     ← 2D 압력 = 피스톤 단위 길이당 힘
닫힌 식:    CAL = R · W · H_PER_L / (40 · SIGMA2_PER_K · m) = 0.02100171   ← W 가 상수에 들어감
```

| 상수 | 값 |
|---|---|
| `SIGMA2_PER_K` (⟨v_x²⟩=⟨v_y²⟩=σ²/T) | 155 논리단위²/(s²·K) |
| `PRESSURE_CAL` (픽셀→atm) | 0.02100171 |
| `dt` / `ω` / `τ` | 1/120 s / 20 s⁻¹ / 0.05 s |
| 기본 상태 | n=1, T=273 K, P_외부=1 atm → **V = 22.4133 L** (표시 22.4 L), 피스톤 충돌 **≈25.0 회/s** |

이 보정으로 `P_측정` 의 기댓값이 **모든 상태에서** 정확히 `nRT/V` 가 된다(특정 상태에 맞춘 것이 아님).

### 1.2 결정론 검사 — 합격 (허용 ±3%)

피스톤 ODE 를 실제로 돌려 정착·스냅까지 거친 뒤 읽은 값이다.

**① 등온 `P×V` (T=273 K, n=1, 추 0~6개)**

| P (atm) | 1.00 | 1.50 | 2.00 | 2.50 | 3.00 | 3.50 | 4.00 |
|---|---|---|---|---|---|---|---|
| V (L) | 22.4133 | 14.9422 | 11.2067 | 8.9653 | 7.4711 | 6.4038 | 5.6033 |
| P×V | 22.4133 | 22.4133 | 22.4133 | 22.4133 | 22.4133 | 22.4133 | 22.4133 |
| 정착 step | 0 | 80 | 77 | 74 | 72 | 71 | 70 |

> 평균 P×V = **22.413300**, 최대 상대편차 = **0.0000 %** (허용 ±3%) → **합격**

**② 등적 `P/T` (V=22.4133 L 고정, n=1, T=100~600 K, 25 K 간격 21점)**

| T (K) | 100 | 200 | 300 | 400 | 500 | 600 |
|---|---|---|---|---|---|---|
| P_이론 (atm) | 0.3663 | 0.7326 | 1.0989 | 1.4652 | 1.8315 | 2.1978 |
| P/T | 0.003663 | 0.003663 | 0.003663 | 0.003663 | 0.003663 | 0.003663 |

> 평균 P/T = **0.00366300**, 최대 상대편차 = **0.0000 %** (허용 ±3%) → **합격**

### 1.3 `P_측정` 검사 — 8개 조합 전부 합격

절차(§10 확정본 그대로): 조합마다 **고정 seed 8개** `[1, 7, 13, 42, 101, 257, 1009, 65537]` 를
각각 **워밍업 2 s 제외 · 측정 25 s** 실행 → seed 당 비율
`r_i = (측정 구간 전체 피스톤 충격량 합 / (W × 측정 시간)) / P_이론` →
8개 `r_i` 의 평균 `r̄` 와 표본표준편차 `s` 로 **95 % t-신뢰구간(자유도 7, t=2.365)** 산출.
**합격 = `|r̄−1| ≤ 0.05` 이고 CI 가 1.00 을 포함하며 조합별 합산 충돌 `N ≥ 2,050`.**

| 조합 | n (mol) | T (K) | V (L) | P_이론 (atm) | 충돌 N | r̄ | s | 95 % CI | 반폭 | 판정 |
|---|---|---|---|---|---|---|---|---|---|---|
| C1 기본 | 1.00 | 273 | 22.4133 | 1.0000 | **4 979** | 0.9988 | 0.0016 | [0.9974, 1.0002] | 0.0014 | 합격 |
| C2 등온압축 | 1.00 | 273 | 5.6033 | 4.0000 | **19 916** | 0.9996 | 0.0008 | [0.9989, 1.0003] | 0.0007 | 합격 |
| C3 저온 | 1.00 | 100 | 8.2100 | 1.0000 | **8 239** | 1.0000 | 0.0016 | [0.9987, 1.0014] | 0.0013 | 합격 |
| C4 고온 | 1.00 | 600 | 49.2600 | 1.0000 | **3 365** | 1.0017 | 0.0058 | [0.9968, 1.0066] | 0.0049 | 합격 |
| C5 소량 | 0.25 | 273 | 5.6033 | 1.0000 | **5 066** | 0.9995 | 0.0018 | [0.9980, 1.0010] | 0.0015 | 합격 |
| C6 다량 | 2.00 | 273 | 44.8266 | 1.0000 | **4 962** | 1.0008 | 0.0071 | [0.9948, 1.0067] | 0.0060 | 합격 |
| C7 등적 고온 | 1.00 | 600 | 22.4133 | 2.1978 | **7 394** | 1.0007 | 0.0023 | [0.9988, 1.0026] | 0.0019 | 합격 |
| C8 등적 저n | 0.50 | 200 | 20.0000 | 0.4105 | **2 416** | 1.0005 | 0.0058 | [0.9956, 1.0053] | 0.0049 | 합격 |

- 8개 조합 모두 `N ≥ 2,050` 을 25 s 기본 측정만으로 충족(자동 연장 발동 없음).
- CI 반폭이 전부 0.006 이하로 **판정 유보 조건(반폭 > 0.05)에 해당하는 조합 없음**.
- 관측된 `r_i` 산포가 §10 의 Poisson 개략치(N=2,050에서 2σ≈4.98 %)보다 훨씬 작다.
  §1.2 의 **배치 정확 정규화**(⟨v_y²⟩ 를 목표값에 정확히 맞춤)가 지배적 분산원을 제거하기 때문 —
  개략치는 측정 시간 산정용이라는 §10 의 단서와 일치한다.

### 1.4 방향 검사 — 합격 (seed 8개 앙상블 평균, 각 15 s)

| 조작 | 충돌 빈도 (회/s) | 평균 충격 | 판정 |
|---|---|---|---|
| **등온 압축** P 1→2 atm (V 22.413→11.207 L, T 고정) | 25.00 → 49.83 (**×1.993**) | 496.07 → 497.78 (**×1.0034**) | 빈도만 ↑ (충격 불변 ±3% 이내) → 합격 |
| **등적 가열** 273→546 K (V 고정) | 25.00 → 35.29 (**×1.412**) | 496.07 → 703.29 (**×1.418**) | 빈도·충격 **둘 다** ↑ → 합격 |

이론값 대조: 등온 압축은 빈도 ×2·충격 ×1, 등적 2배 가열은 둘 다 ×√2 = 1.414. 실측이 정확히 일치.
학생 화면에서도 동일하게 관측됨(⑤ 실제 조작 로그: 273→498 K 에서 빈도 26.0→37.0, 평균충격 4.73→6.36).

### 1.5 피스톤 정착 (§1.3 이산 실측)

세미-임플리시트 오일러, `dt=1/120`, `τ=0.05 s`, 정착 조건 `|ΔV|/V_eq < 10⁻⁴` **이고** `|dV/dt|/V_eq < 10⁻³ s⁻¹`.

| 상태 변화 폭 A | 정착 step | 시간 | τ 배수 | 기획서 기재값 |
|---|---|---|---|---|
| A = 1 | **86** | 0.717 s | 14.3 τ | 86 step ≈ 0.72 s ✔ 일치 |
| A = 11 (V 60→5 L) | **106** | 0.883 s | 17.7 τ | 106 step ≈ 0.88 s ✔ 일치 |

기획서 §1.3 이 명시한 이산 실측값과 **step 단위까지 정확히 일치**한다.

### 1.6 Edge Function 검증 27건 — 전부 통과

하위 호환 6건(대표 payload: **원본 사진 4장 × 5 MB(최악값) + 텍스트 2개 = 본문 25.43 MB**, `contextId` 미전송) /
`ideal_gas` 5건 / 서버 방어 13건 / IP 빈도 제한 3건.

---

## 2. §8.3 체크리스트 자체 점검

브라우저 실측(Chrome, iframe 내부 뷰포트를 정확한 px 로 만들어 측정).

| # | 항목 | 결과 | 근거 |
|---|---|---|---|
| 1 | 추(±0.5 atm)·온도(±25 K)·n(±0.25 mol) 버튼 **키보드 조작(`←→`)** | ✅ | 스테퍼에 포커스 후 `ArrowRight` → 1.00→1.50 atm, `ArrowLeft` → 1.00 atm 복귀 확인 |
| 2 | 포커스 링 라임 | ✅ | `:focus-visible{outline:3px solid var(--lime)}` — 모든 버튼·입력에 적용 |
| 3 | **터치 타깃 44px 이상** | ✅ | 초기 구현에서 `.btn.sm`·`.minibtn`·삭제 버튼이 38px → **44px 로 수정**. 재측정 결과 44px 미만 요소 **0개** |
| 4 | ≤720px 폭 1열 접기(캐스케이드 ③) | ✅ | 360×512 → 1열, 계기판 `position:absolute` 오버레이 칩 |
| 5 | 내부 높이 ≤470px → 저높이 2열(④)이 ③을 덮음 | ✅ | 640×288·800×288 → `.simblock{display:grid}` 2열, 캔버스 177×232 |
| 6 | 정답/오답 **색 + 아이콘 + 문구** 3중 표시 | ✅ | `✔`/`✕` + 라임/시그널 배경 + 한국어 문구 (`#interceptVerdict`·`#rVerdict`·`#a3Verdict`·AI 배지) |
| 7 | 계기판 숫자 `tabular-nums` | ✅ | `font-variant-numeric: tabular-nums` 실측 확인 |
| 8 | 캔버스 `role="img"` + `aria-label` 상태 요약 | ✅ | 예: "압력 1.00 기압, 부피 22.4 리터, 온도 273 켈빈(섭씨 0도), 기체 양 1.00 몰, 입자 40개, 피스톤 충돌 초당 25.0회, 평형 상태." (약 0.75 s 주기 갱신) |
| 9 | iframe 안 스크롤 정상 | ✅ | `height:min(80vh,760px)` 컨테이너 안에서 내부 스크롤·sticky 컨트롤 정상 |
| 10 | 세로 360×640: 캔버스+컨트롤 동시 표시 | ✅ | 내부 512: 캔버스 243×312 + 컨트롤 바 140px + 진행 띠 44px = 496 ≤ 512 |
| 11 | 가로 640×360: 2열 전환·저높이 배너 | ✅ | 내부 288 → ④ 2열. 내부 **219px → 배너 표시**, **221px → 배너 없음**(경계 220px 동작) |
| 12 | 800×600(내부 ≈480): 캔버스 축소 후 동시 표시 | ✅ | ②만 매칭 → 2열, 캔버스 241×316 + 바 96 + 띠 44 = 456 ≤ 480 |
| 13 | 800×360(내부 ≈288): 폭>720 이어도 ④ 적용 | ✅ | ④ 2열, 캔버스 177×232 |
| 14 | **소수 px 경계 공백 없음** | ✅ | 내부 **470.5px** → ①/② (④ 미적용) / 폭 **720.5px** → ①/② (③ 미적용) / 폭 721·471 → ①/② / 폭 720·470 → ④ — 규칙 공백 0 |

**동시 표시 판정은 4개 SimBlock(①③④⑥) × 5개 뷰포트 전부에서 통과**
(캔버스·잠금 라벨·조작 스테퍼·기록 버튼이 모두 뷰포트 안에 완전히 들어옴).

### 2.1 그 밖에 실측 확인한 사양 조항

| 조항 | 확인 내용 |
|---|---|
| §1.1 모드 3종 + 반응 변수 | 등온 P→V, 등압 T→V, 등적 T→P, n→V 전부 동작. **`V/T` 가 모든 샤를 점에서 정확히 0.0821**, `P/T` 가 모든 게이뤼삭 점에서 0.003663 |
| §1.4 범위 정합 규칙 | T 끝점 스냅 실측 **248→223→…→123→100** (98 이 아니라 100 으로 스냅) · 600 K 상한 · n+ 차단 배지 "V가 60 L을 넘어요" · 등적 냉각이 **150 K(P=0.25 atm)** 에서 차단 "P가 0.25 atm보다 작아져요" |
| §1.4 n 이산 스텝 | 1.00 mol(40개) → 1.50 mol(60개), V 22.4→33.6 L (아보가드로), 입자 수 항상 정수 |
| §3.1 `unlockedStep` 불신 | `unlockedStep=6` 위조 + ② 조건만 비움 → **재평가로 2 로 복원**, ②는 편집 가능 상태로 펼침 |
| §3.1 로드 검증 | 깨진 JSON → `_bak` 보존 후 안전 기본값 시작 · `pMeasured=99.5`(범위 밖) 는 **손상으로 보지 않고 보존** |
| §3.1 공용 태블릿 | 저장된 `student.sid` ≠ 주입 학번 → 자동 복원하지 않고 새로 시작 |
| §3.2 `_anon` 이관 | `_anon` 진행 → 학번 입력 → 대상 키 저장·재확인 → 활성 키 교체 → `_anon` 삭제. 이후 자동저장은 대상 키에만(`_anon` 재생성 없음) |
| §3.2 교사 미리보기 | `isAdmin` → `_teacher` 키, 배지 표시, **전 단계 게이트 우회**(내보내기 버튼도 활성) |
| §3.3 내보내기 해시 | 내보내기 후 답 1글자 수정 → `exportedAt` 즉시 `null`, "내용을 수정했어요" 표시, ⑥ 완료 게이트 해제 |
| §3.3 파일명·`<a download>` | `이상기체_20900_김민수_20260726.json` (팝업 없이 다운로드) |
| §5⑥ `PV/nT` | 12개 점 전부 표시 4자리에서 **0.0821** |
| §6 "서로 다른 점" | 같은 P 를 두 번 기록해도 1개로 카운트("지금 3개") |
| §6 이전 단계 읽기 전용 | 다음 단계 해제 시 이전 단계의 답·기록·삭제 UI 비활성(시뮬레이터 관찰은 계속 가능) |
| §8.2 화면 밖 정지 | 블록이 화면 밖이면 물리 정지 + 기록 버튼 "시뮬레이션이 보일 때만 기록" |
| §8.4 요청 형식 | 실제 전송 payload 확인: `contextId:"ideal_gas"`, `kind` 전부 `text`, **`expect` 필드 없음**, `apikey` 헤더 포함 |
| §3.3 인쇄 | 6개 그래프가 `toDataURL('image/png')` 이미지로 삽입되고 `afterprint` 에 정리됨 |

### 2.2 §8.4 필요성의 실측 근거 (중요)

구현 도중 **현재 배포되어 있는(수정 전) Edge Function** 으로 실제 호출이 나갔고,
`predict.reason`·`boyle.q1`·`boyle.q2`·`charles.relation` 4개가 **`ok=false` ("실험과 무관한 내용입니다.")** 로 돌아왔다.
스펙트럼 전용 `SYSTEM_TEXT` 하드코딩 때문이라는 §8.4 의 지적이 **실제로 재현**된 것이다.
`REQUIRE_AI_PASS=false` 라 진도는 막히지 않았지만, **이 Edge Function 수정을 배포하지 않으면
이상기체 페이지의 서술 응답에 계속 오탐 경고가 뜬다.**

---

## 3. 구현 중 내린 재량 결정

기획서가 값을 지정하지 않았거나, 지정 값끼리 충돌해 판단이 필요했던 항목이다.

### 3.1 물리 상수 (기획서가 "닫힌 식으로 유도" 만 지정)

| 결정 | 값 | 근거 |
|---|---|---|
| 실린더 내부 폭 `W` | 260 논리단위 | 320 논리폭 − 좌우 여백 30씩 |
| 기체 기둥 스케일 `H_PER_L` | 6.1 (=366/60) | V=60 L 일 때 기둥 높이가 실린더 내부(366)를 꽉 채우도록 |
| `SIGMA2_PER_K` | 155 | 기본 상태 피스톤 충돌이 **≈24~25 회/s** 가 되도록 역산 — §1.2·§10 이 예시로 든 "≈24회/s" 와 맞춤. 결과적으로 25 s×8 seed 로 N≈4,979 확보 |
| 입자 질량 `m` | 1 (임의 단위) | 보정 상수에 흡수됨 |

### 3.2 물리 근사

- **입자는 물리적으로 점입자**로 다루고(반지름 0), 화면에는 반지름 4로 그린다.
  반지름만큼 안쪽으로 밀어 넣으면 유효 높이가 `H−2r` 이 되어 §1.2 의 해석적 보정이 깨지기 때문.
  그리기 좌표만 벽 안쪽으로 클램프한다.
- **피스톤이 쓸고 지나간 입자는 위치만 되돌리고 속도는 건드리지 않는다.**
  집단 `⟨v_y²⟩` 를 보존해 보정이 계속 성립한다. 이동 중에는 어차피 측정 창을 비우므로(§1.2) 측정에 영향 없음.
- **배치 제거는 LIFO(가장 최근 배치부터).** 각 배치가 개별적으로 정확 정규화되어 있으므로
  어떤 부분집합을 남겨도 `⟨v_y²⟩` 가 목표값을 유지한다(§1.2 요구사항 충족).
- **이동평균 창 구현**: 2 s 를 1/12 s 짜리 bin 24개로 나눈 링버퍼. "창이 찬다" = bin 24개 충전 완료.

### 3.3 데이터·UI

| 결정 | 내용 |
|---|---|
| 정합값 선택 함수 | `boyle`·`charles` → `pExternal`, `gaylussac`·`combine` → `pTheory` (§1.2 규칙). 피스톤 자유 모드에서는 정착 후 두 값이 수치적으로 같다 |
| 로드 검증의 P 범위 | `pExternal` 은 **입력 범위 1.0~4.0**, `pTheory` 는 **표시 범위 0.25~4.0** 로 나눠 검사(§1.4 각주). 등적에서 `pExternal` 을 `pTheory` 로 동기화하지 **않는** 이유 |
| 해시 함수 | FNV-1a 32bit (자립형 제약상 crypto 의존 없이 결정적 · `textHash`·`exportedHash` 공용) |
| 초기화 | `location.reload()` 대신 **메모리 상태 리셋 + 재렌더**. `srcdoc` iframe 에서 reload 동작이 불확실하기 때문 |
| 읽기 전용 범위 | 이전 단계에서 **답 수정·점 기록·점 삭제만** 잠그고 시뮬레이터 관찰·일시정지·속도는 계속 허용(§6 은 "점 삭제·답 수정 UI" 를 지정) |
| ⑥ 자유 모드 컨트롤 | 현재 모드에서 조작 불가한 스테퍼는 흐리게가 아니라 **접는다**(높이 절약 + "무엇을 고정하고 무엇을 미는가" 강조) |
| 그래프 추세선 | 보일 = 평균 `k=P·V` 쌍곡선, 1/V·샤를·게이뤼삭 = 최소제곱 직선. 샤를 ℃ 축에서만 **V=0 까지 주황 점선 외삽 + x절편 라벨** |
| 사진 없음 | §3.1 대로 스펙트럼의 용량 fallback 미구현 |

### 3.4 §2.1 레이아웃 — 기준을 만족시키기 위한 조정

기획서 §2.1 의 판정 기준은 **"캔버스 + 핵심 컨트롤 결합 높이 ≤ 내부 높이 − 진행 띠 − 24px"** 이고,
그 예산은 "터치 컨트롤 **44px 1줄**" 을 가정한다. 그런데 이 페이지의 컨트롤은
잠금 라벨 + 스테퍼(최대 3종) + 기록 + 재생/속도까지 있어 1줄로 끝나지 않는다.
그래서 다음을 조정했다(레이아웃 **선택 규칙**인 max-\* 캐스케이드 4단계는 기획서 그대로 유지).

1. **핵심 컨트롤을 한 줄로 묶음** — `잠금 라벨 + 스테퍼 + 기록 버튼` 을 `.ctlrow.core` 하나로 합치고,
   모드 선택·재생/속도는 그 뒤에 둔다(DOM 순서 = 중요도 순서).
2. **컨트롤 바에 예약 높이 상한**(`--ctl-reserve`)을 두고 넘치면 **바 안에서만 스크롤**:
   ② 96px · ③ 140px · ④ 오른쪽 열 전체. 캔버스 높이 계산과 **같은 상수**를 쓰므로 합이 항상 뷰포트 안에 들어온다.
3. **③(≤720px 폭)의 캔버스는 `280×360` 을 기본으로 하되, 내부 높이가 모자라면 비율을 지키며 축소**
   (`min(360px, 100vh − 진행띠 − 예약 − 16px)`). 예: 내부 640 → 280×360(기획서 값 그대로),
   내부 512 → 243×312. **기획서가 제시한 크기는 들어갈 때 항상 그대로 쓰고, 못 들어갈 때만 줄인다.**
4. ④(저높이 2열)에서 `.simwrap{display:contents}` 로 캔버스·계기판·컨트롤을 `.simblock` 그리드에 직접 올려
   **"왼쪽 캔버스 / 오른쪽 컨트롤+계기판 칩"** 배치를 실현(§2.1 ④ 문구 그대로). 측정 칩은 아래 스크롤로 보냄.
5. ②③ 에서 줄 라벨(`추`/`온도`/`펌프`)과 스테퍼 캡션(`추 6개`/`40개`)을 숨김 — 값·단위와 `aria-label` 이 남아 있고,
   추 개수는 실린더 그림에, 입자 수는 계기판에 그대로 보인다.
6. 진행 띠 실측 높이를 로드·리사이즈 때 `--rail-h` 로 갱신(고정 44px 가정이 기기별로 어긋나는 것 방지).

**남는 경계 1곳(기획서 자체 수치에서 유래)**: 내부 높이 **220~232px** 구간에서는
④의 캔버스 하한 180px + 진행 띠 44px + 여백이 뷰포트를 넘는다(배너 임계값이 220px 이므로 배너도 안 뜬다).
이 구간은 컨트롤 바가 자체 스크롤되어 조작 자체는 가능하다. 실기기에서 나오기 어려운 크기라 그대로 두었고,
**임계값을 220 → 240px 로 올리면 완전히 사라진다**(기획서 수치 변경이므로 임의로 바꾸지 않음).

### 3.5 Edge Function

| 결정 | 내용 |
|---|---|
| `mediaType` 허용목록 | `image/jpeg`·`png`·`webp` + **`heic`·`heif` 추가** — 아이폰 기본 촬영 포맷이라 하위 호환(§8.4 "실측되는 타입을 포함해 확정")을 위해 넣음 |
| IP 빈도 제한 | 인스턴스 로컬 `Map` 슬라이딩 윈도(외부 저장소 없음). Edge 인스턴스가 여러 개면 실효 상한이 그만큼 늘어난다 — 남용 차단용 1차 방어라는 §8.4 취지에 부합 |
| 응답 검증 | 요청 `items` 에 없는 key·중복 key 를 서버에서 제거. 클라이언트는 빠진 key 를 `unavailable`(통과)로 처리 |
| 본문 상한 32 MB | 코디네이터 결정(§4.1) — 하위 호환 우선. 장당 상한을 통과하는 사진 4장이 합계에서 거부되지 않도록 20 → 32 MB |
| 순수 구간 분리 | 검증 로직을 `PURE VALIDATION BEGIN/END` 로 감싸 Deno API 를 쓰지 않게 유지 → Node 테스트가 같은 코드를 그대로 실행 |

---

## 4. 코디네이터 확인 사항 / 배포 전 남은 작업

### 4.1 ✅ 본문 상한 20 → 32 MB (코디네이터 결정, 반영 완료)

§8.4 확정 수치는 **사진 1장 `imageBase64` ≤ 7,000,000자**(원본 ≈5 MB)이고 **본문 전체 ≤ 20 MB** 였다.
그런데 현행 스펙트럼 페이지는 `FileReader.readAsDataURL` 원본을 **리사이즈 없이 4장** 보낸다.

| 원본 사진 1장 | base64 1장 | 4장 본문 | 구 상한 20 MB | 신 상한 32 MB |
|---|---|---|---|---|
| 2.5 MB | 3.33 M자 | 12.7 MB | 통과 | 통과 |
| 3.7 MB | 4.93 M자 | 19.7 MB | 아슬아슬 | 통과 |
| 5.0 MB | 6.67 M자 | **25.4 MB** | **거부(400)** | **통과** |

즉 **장당 상한은 통과하는데 4장을 합치면 본문 상한에 걸리는 구간**이 있었다.
실기기 사진 실측이 이 환경에서 불가능해 임의로 바꾸지 않고 코디네이터에게 문의했고,
**(b) 32 MB 로 상향** 결정을 받아 반영했다(기획서 §8.4 도 v1.14 에서 32 MB 로 갱신됨).

- 근거: 하위 호환 우선 원칙 — **장당 7,000,000자를 통과하는 요청은 4장 합계도 통과해야 한다.**
  4 × 7 M자 = 28 MB + JSON 오버헤드 여유 → **32 MB**.
- 회귀 테스트도 **최악값(장당 5 MB × 4장 = 본문 25.43 MB)** 으로 올려 통과를 확인했다.

### 4.2 배포 전 남은 작업(이 워커 범위 밖 — 지시대로 하지 않음)

1. `supabase functions deploy check-experiment-response` — **하기 전에는 §2.2 의 오탐이 계속된다**
2. HTML 을 스토리지 같은 경로로 업로드(§9.3) 후 `단원-상세.html` 아코디언에서 iframe 실측
3. §9.4 배포 선행 조건 — `materials` 테이블·스토리지 버킷 쓰기를 **학생 세션으로 시도해 거부되는지** 실측
   (실패 시 §9.4 후속 과제가 배포 선행 조건으로 승격)
4. git commit
